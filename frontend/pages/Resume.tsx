import React from "react";

const Resume = () => {
  return (
    <section
      className="w-full h-[60rem] bg-gray-100  dark:bg-dot-white/[0.2] bg-dot-black/[0.2]"
      id="resume"
    >
      <div className="[mask-image:radial-gradient(ellipse_at_center,transparent_20%,black)] w-full h-full"></div>
    </section>
  );
};

export default Resume;
